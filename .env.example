# 이 파일을 .env 로 복사한 뒤 실제 키를 채우세요. (.env 는 커밋 금지)
#   cp .env.example .env

# Anthropic(Claude) API 키. https://console.anthropic.com 에서 발급.
# 지금은 비워둬도 됩니다 — 골격을 먼저 만들고, 실제 구동할 때 채웁니다.
ANTHROPIC_API_KEY=
