# 리눅스에서 실행되는 파일은 줄바꿈을 항상 LF로 강제한다.
# 윈도우 자동 변환(CRLF)이 끼면 컨테이너 안에서 'bash\r' 오류로 스크립트가 깨진다.
*.sh          text eol=lf
Dockerfile*   text eol=lf
gradlew       text eol=lf

# 반대로 윈도우 배치 파일은 CRLF 유지.
*.bat         text eol=crlf

# 바이너리로 취급 (줄바꿈 변환 금지).
*.jar         binary
